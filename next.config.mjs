/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['serconomar.com',
          'cdn.worldvectorlogo.com',
          'worldvectorlogo.com',
         'www.geverel.com',
        'media.licdn.com'
          
        ], // Agrega el dominio aquí
      }
};

export default nextConfig;
